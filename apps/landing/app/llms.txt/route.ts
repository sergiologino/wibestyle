import { seoPages } from "@/content/seo-pages";
import { pricing, siteConfig } from "@/lib/site";

export function GET() {
  const lines = [
    `# ${siteConfig.name}`,
    "",
    siteConfig.description,
    "",
    "## Возможности",
    "- AI-примерка одежды по фото и ссылке с маркетплейса",
    "- Примерка причёсок, стрижек и цвета волос по фото",
    "- Генерация видео примерок для подходящих результатов",
    "- Скачивание медиа и шаринг образов в соцсети, сторис и мессенджеры",
    "- Реферальная система с бонусными примерками за приглашения",
    "- Виртуальный макияж онлайн по фото готовится к запуску",
    "- Персональный нейростилист для полного образа: одежда, обувь, аксессуары, причёска и макияж; сценарий полного образа в работе",
    "- Wildberries, Ozon, Яндекс Маркет и другие",
    "- Сохранение и шеринг образов",
    "- Приватный режим (скрытие лица)",
    "",
    "## Цена",
    "- Нет подписки по срокам: оплата идёт за количество примерок",
    `- 20 примерок: ${pricing.tryOn20Rub} RUB`,
    `- 50 примерок: ${pricing.tryOn50Rub} RUB, рекомендуемый пакет`,
    `- 100 примерок: ${pricing.tryOn100Rub} RUB`,
    "",
    "## Ключевые страницы",
    ...seoPages.map((p) => `- ${siteConfig.domain}${p.slug} — ${p.h1}`),
    "",
    "## Будущие функции",
    "Макияж и полный образ — в ближайших релизах. Причёски, стрижки и цвет волос уже продвигаются как отдельный сценарий.",
  ];

  return new Response(lines.join("\n"), {
    headers: { "Content-Type": "text/plain; charset=utf-8" },
  });
}
