"use client";
import { useState } from "react"; import { apiBaseUrl } from "@/lib/api-media"; import { useAppSession } from "@/components/providers/AppSessionProvider";
export default function HairstylePortraitManager(){const {accessToken}=useAppSession();const [url,setUrl]=useState<string|null>(null);const [busy,setBusy]=useState(false);async function pick(e:React.ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f||!accessToken)return;setBusy(true);const b=new FormData();b.append("portrait",f);const r=await fetch(`${apiBaseUrl()}/api/v1/profile/hairstyle-portrait`,{method:"POST",headers:{Authorization:`Bearer ${accessToken}`},body:b});if(r.ok)setUrl(`${apiBaseUrl()}/api/v1/profile/hairstyle-portrait/image`);setBusy(false)}return <section className="rounded-[24px] border border-[#ffd1ed] bg-white p-5"><h2 className="text-display-md text-2xl">Портрет для причёсок</h2><p className="text-body mt-2">Отдельный крупный портрет от макушки до плеч. Он не заменяет аватар для одежды.</p>{url?<img src={url} alt="Портрет для причёсок" className="mt-4 size-32 rounded-2xl object-cover"/>:null}<label className="mt-4 inline-block cursor-pointer rounded-2xl bg-[#fff4fb] px-4 py-3 text-sm font-medium text-[#ff1fa2]"><input type="file" accept="image/*" className="sr-only" onChange={pick}/>{busy?"Загружаем…":url?"Заменить портрет":"Загрузить портрет"}</label></section>}
