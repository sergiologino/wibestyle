import { DEFAULT_FEATURE_FLAGS } from "@wibestyle/shared-types";

export const mobileFoundation = {
  appName: "Я на стиле",
  primaryFlow: "single-item-photo-try-on",
  featureFlags: DEFAULT_FEATURE_FLAGS,
};
