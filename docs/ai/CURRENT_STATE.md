# Current State

## Фактическое состояние
- Next.js 16 лендинг полностью переработан по прототипу `yanastyle-landing`.
- Многостраничная SEO-структура (17 подстраниц + главная).
- Яркий визуал: hero до/после, hot-band, галерея примеров, шаги, категории, future, отзывы, CTA.
- Регистрация раннего доступа через API с учётом первых 100 мест (−50% на год, 6990→3495 ₽).
- Яндекс.Метрика через `NEXT_PUBLIC_YANDEX_METRIKA_ID`.
- `robots.txt`, `sitemap.xml`, `llms.txt`, `llms-full.txt`.
- Автотесты (7) проходят в сборке.

## Ключевые пути
- Главная: `app/page.tsx`, `components/home/HomePage.tsx`
- SEO-страницы: `content/seo-pages.ts`, `app/[...slug]/page.tsx`
- Изображения: `content/image-slots.ts`, `public/assets/`
- Лиды: `app/api/leads/route.ts`, `data/leads.json`
- Стили прототипа: `app/prototype.css`

## Последние правки (вёрстка SEO-страниц)
- Отдельные шаблоны: `how-it-works` (Как работает), `editorial` (Подбор образа, Макияж).
- Hero-band + визуальные полосы, карточки вместо «стены текста», до/после в 2 колонки.
- Ранний доступ: полноценный градиентный блок с призывом «первые в новой эре стиля».
- Макияж: реальные PNG вместо SVG-овалов (временно female-card; слоты для замены в image-slots).

## Предыдущие правки (ревью)
- Обновлены тексты: «нейростилист» / «виртуальный персональный стилист» вместо «AI».
- Визуалы на `/ai-primerka`, `/podbor-obraza`, `/makiyazh` и других ключевых страницах.
- Макияж: 2 пары до/после (вечерний + лёгкий макияж); подбор образа — мозаика look.
- Блоки «память стилиста» и «оценка лука по фото» на главной и SEO-страницах.
- Ранний доступ оформлен в брендовом градиенте; маркетплейс в форме — необязательный.
- FAQ и нейропримерка: живые формулировки, размер по пропорциям, галерея приложения.
- Подбор образа: запуск в начале июля 2026.

## Что дальше
- Заменить placeholder SVG макияжа (`public/assets/makeup-*.svg`) на реальные фото.
- Подключить production backend/CRM для лидов.
